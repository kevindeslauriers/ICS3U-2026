package day2;

public class ExampleTwoB {
    public static void main(String[] args) {
        // int and double are primitive types

        int numApples = 7;      // allLowerCaseExpectTheStartOfEveryWordAfterTheFirstWord
                                //camelCase is used for variables 

        System.out.println(numApples);
        numApples = 10;             // we can change the value of a variable
        System.out.println(numApples);

        final int numBananas = 5;       // final makes a variable a constant so it cannot be changed
        // numBananas = 6;  // error cannot change it

        final int numStudents;

        numStudents = 24;
        // numStudents = 23; // once a constant has been initialised it cannot be changed
        






    }
}
